workflows:
  android-build:
    name: 798 Deenar AI Android Build

    environment:
      node: 22
      java: 17

    scripts:
      - npm install
      - npm run build
      - npx cap sync android
      - cd android && ./gradlew assembleDebug

    artifacts:
      - android/app/build/outputs/apk/debug/*.apk
